{
  "name": "@relay/e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"e2e se ejecuta con 'pnpm e2e' desde la raíz\" && exit 0",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@relay/shared": "workspace:*",
    "typescript": "^5.9.3",
    "@types/node": "^22.19.19"
  }
}
