API_URL=http://localhost:4000

# Sólo cuando el servidor de Next alcanza al API por una red privada distinta
# de la que usa el navegador (por ejemplo, dentro de docker-compose).
# Sin definir, cae a API_URL.
# API_INTERNAL_URL=http://api:4000
