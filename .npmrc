# Los apps importan sólo lo que declaran en su package.json. Evita el bug
# clásico del monorepo: funciona en local por hoisting y truena en el build.
strict-peer-dependencies=false
auto-install-peers=true
